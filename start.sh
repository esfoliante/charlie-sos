# source .venv/bin/activate
# python3 -m pip install -r requirements.txt

# We're assuming you already built the image.
# If not, please first run the build.sh file

docker run -it --rm charlie-sos:latest
